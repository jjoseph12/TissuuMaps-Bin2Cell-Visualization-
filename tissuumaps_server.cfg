import os

# Tell the Flask server to behave like the desktop build so the Plugins tab is visible.
isStandalone = True

# Pin the plugin in ~/.tissuumaps so your custom JS/Python plugins are found
# even if the cluster job ends up running under a different $HOME.
PLUGIN_FOLDER_USER = os.path.join(os.path.dirname(__file__), "plugins")
